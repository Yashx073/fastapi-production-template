apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: drift-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: drift-monitor
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
